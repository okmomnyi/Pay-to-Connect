# HTTPS config — not yet active (certbot must run first to create cert files)
# When ready, copy the full config here from:
#   nginx/sites-available/pay-to-connect-letsencrypt
# Then: docker compose exec nginx nginx -s reload
